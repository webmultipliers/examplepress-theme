/**
 * Build — Connections tab: form, save, test buttons, auth callbacks.
 *
 * Note: Connection settings + Troy auth + GitHub App callbacks are
 * handled by inline scripts in the PHP page template (immune to
 * browser cache of this JS file). This module is a placeholder for
 * when those inline scripts are migrated.
 */

// The connections tab currently uses inline PHP-injected scripts for:
// - Save connection settings form
// - Test GitHub connection button
// - Test Troy connection button
// - GitHub App install callback
// - Troy auth callback
//
// These will be migrated in Phase 5 of the rework plan.
