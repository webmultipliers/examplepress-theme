/**
 * Build — Connections tab: form, save, test buttons, auth callbacks.
 *
 * Connection settings, Troy auth, and GitHub App callbacks are
 * currently handled by inline scripts in the PHP page template.
 */
